[package]
name = "shm_ring"
version = "0.1.0"
edition = "2021"
description = "A directional ring buffer for message passing between shared memory"
author = "Thomas Johnson"

[dependencies]
libc = "*"
nix = "*"

